AIRTABLE_TOKEN=pat...
AIRTABLE_WORKSPACE_ID=wsp...
# Once the base is created, the script saves the ID here automatically.
# You can also set it manually if the base already exists.
AIRTABLE_BASE_ID=
